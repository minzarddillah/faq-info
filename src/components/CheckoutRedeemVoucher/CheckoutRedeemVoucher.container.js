import { connect } from 'react-redux';
import CheckoutRedeemVoucher from './CheckoutRedeemVoucher.screen';


const mapStateToProps = state => ({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(CheckoutRedeemVoucher);

