import { connect } from 'react-redux';
import QuestInfo from './QuestInfo.screen';


const mapStateToProps = state => ({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(QuestInfo);

